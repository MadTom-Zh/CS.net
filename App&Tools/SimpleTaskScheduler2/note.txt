


界面

近期任务
显示设定周期内会发生的任务；
* 只保存最后执行任务的时间点，重启程序后，用此时间点检查未执行的任务、提示并写日志，不再询问是否执行；

周期计划
设定 一次性，每天，每周，每月，特殊周期 的任务；
分组显示，其中一次性任务，如果过期，则显示为灰色背景；
每次修改应用后，重新生成近期任务；

设定
显示近期任务的天数，默认8；
时钟周期，默认最小1s，最大300s；




机制






附加程序：
延迟启动器，给定参数，延迟一段时间后执行cmd命令；
【消息框】